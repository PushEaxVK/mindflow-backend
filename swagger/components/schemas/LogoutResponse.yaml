LogoutResponse:
  type: object
  required: [status, message, data]
  properties:
    status:
      type: integer
      example: 200  
    message:
      type: string
      example: Successfully logged out!
    data:
      type: object
      required: [message]
      properties:
        message:
          type: string
          example: User logged out successfully